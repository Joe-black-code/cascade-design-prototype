# «Каскад»: дорожная карта дизайна и интерактивного прототипа

> Живой документ проекта. Используется как единая точка восстановления контекста между чатами и рабочими сессиями.

## 1. Паспорт документа

- Проект: редизайн сайта ООО «ЗСО КАСКАД» (`cascade.su`).
- Текущий этап: UI-дизайн Главной и Каталога с адаптивом и интерактивной демонстрацией.
- Формат результата: размещённый HTML/CSS/JavaScript-прототип.
- Количество направлений: обязательны 2, целевой результат — 3 полноценных визуальных концепции.
- UI-kit: создаётся кодом на отдельной странице после выбора концепции.
- Последнее обновление: 2026-09-14.
- Текущая контрольная точка: CP-1.1, CP-1.2A и CP-1.2B завершены; CP-1.2 в работе, CP-1.2C не начат.

## 2. Как вести документ

После каждого значимого этапа или подэтапа необходимо обновлять:

1. статус дорожной карты;
2. реестр принятых решений;
3. список созданных артефактов и ссылок;
4. открытые вопросы и блокеры;
5. журнал изменений в конце документа.

Решения не изменяются молча. При пересмотре нужно указать:

- какое решение меняется;
- почему оно меняется;
- какие страницы, компоненты и материалы затрагиваются;
- что требуется обновить.

Статусы:

- `Не начато` — работа ещё не выполнялась;
- `В работе` — активный этап;
- `На согласовании` — ожидается решение;
- `Завершено` — результат проверен и принят как текущая база;
- `Отложено` — сознательно вынесено за пределы текущего этапа;
- `Заблокировано` — продолжение невозможно без внешнего решения или материала.

## 3. Зафиксированный контекст

### 3.1. Бизнес и позиционирование

- «Каскад» должен восприниматься как российский завод систем ограждений полного цикла, а не торговый дом или посредник.
- Рабочая бренд-роль: «Завод-Гарант» — надёжность, инженерность, защита, контроль и ответственность.
- Основные сценарии сайта:
  - найти и выбрать конкретную продукцию;
  - подобрать комплексное решение под объект;
  - передать ТЗ или смету для расчёта;
  - получить инженерные материалы и документы.
- Основной фокус — B2B. Сценарий частного клиента сохраняется, но не должен доминировать.
- Красный используется точечно как сигнальный цвет.

### 3.2. Данные аудита, влияющие на дизайн

- Около 70% трафика приходится на смартфоны.
- 72,2% входов приходится на страницы 2D- и 3D-сетки.
- Главная получает только 2,7% входов, но имеет высокую конверсию.
- Компоненты первого этапа должны быть пригодны для последующего переноса на продуктовые страницы.

### 3.3. Текущий объём

- Главная страница.
- Каталог продукции.
- Сквозной хедер.
- Четыре десктопных мегаменю.
- Мобильное drill-down-меню.
- Сквозной футер.
- Модальное окно расчёта.
- Демонстрационные формы и состояния.
- Две обязательные и одна желательная визуальная концепция.
- Адаптив всех концепций.

### 3.4. Не входит в текущий этап

- интеграция с Битрикс24;
- production-разработка под 1С-Битрикс;
- серверная обработка форм;
- реализация всех страниц сайта;
- окончательная SEO-редактура текстов;
- production-оптимизация кода;
- финальный UI-kit до выбора концепции.

## 4. Архитектурные принципы прототипа

### 4.1. Единый источник сквозных компонентов

Хедер, футер, модальные окна и другие сквозные элементы должны существовать в исходниках в одном экземпляре и подключаться ко всем страницам автоматически.

Базовое решение:

- использовать шаблонные partial-компоненты на этапе сборки;
- на выходе получать обычные статические HTML-файлы;
- не загружать хедер и футер через runtime `fetch()`;
- не дублировать вручную разметку сквозных компонентов в каждой странице;
- не завязывать отображение основного контента на выполнение JavaScript.

Предварительная структура:

```text
src/
  pages/
    index.html
    catalog.html
    ui-kit.html              # после выбора концепции
  partials/
    site-header.html
    mega-menu-products.html
    mega-menu-turnkey.html
    mega-menu-special.html
    mega-menu-company.html
    mobile-navigation.html
    site-footer.html
    estimate-modal.html
    demo-notice.html
  styles/
    tokens.css
    base.css
    layout.css
    components.css
    responsive.css
    themes/
      guarantor.css
      system.css
      contour.css
  scripts/
    navigation.js
    modal.js
    forms.js
    slider.js
    theme-switcher.js
  assets/
    fonts/
    icons/
    images/
dist/
  index.html
  catalog.html
  assets/
```

Конкретный минимальный шаблонизатор выбирается при создании рабочего каркаса. Критерий выбора — простая локальная сборка без runtime-зависимости на хостинге.

### 4.2. Общая разметка для трёх концепций

- Семантика, тексты, порядок блоков и интерактив остаются общими.
- Визуальное направление задаётся атрибутом `data-theme`.
- Темы могут менять не только цвета, но и композицию блоков через CSS Grid, размеры, пропорции медиа и декоративные слои.
- Допустимы небольшие тематические элементы в общей разметке, если они скрываются в неподходящих концепциях.
- Не допускаются три независимо скопированных набора страниц.

Рабочие идентификаторы:

- `guarantor` — «Завод-Гарант»;
- `system` — «Инженерная система»;
- `contour` — «Стальной контур».

### 4.3. Сквозные состояния

Для демонстрации предусматриваются:

- обычное состояние;
- hover;
- keyboard focus;
- active/pressed;
- открытое и закрытое меню;
- открытая и закрытая модалка;
- ошибка заполнения формы;
- успешная демонстрационная отправка;
- сообщение для разделов, отсутствующих в текущем этапе.

## 5. Политика сетки и адаптива

Сетка фиксируется в два шага.

### Шаг 1 — до визуального слоя

Сейчас фиксируются устойчивые правила:

- 4 условные колонки на мобильных экранах;
- 8 условных колонок на планшетах;
- 12 условных колонок на десктопе;
- единая шкала отступов и промежутков;
- fluid-контейнер с ограничением максимальной ширины;
- отсутствие горизонтального скролла от 360 px;
- независимые правила для общего layout и внутренних сеток карточек;
- возможность использовать container queries для переиспользуемых компонентов;
- обязательные контрольные ширины: 360, 390, 768, 1024, 1440 и 1920 px.

### Шаг 2 — во время разработки визуального слоя

После выбора шрифтов, размеров логотипа, пропорций фотографий и плотности карточек уточняются:

- точная максимальная ширина контейнера;
- реальные точки перестроения сеток;
- breakpoint мобильного хедера;
- количество колонок Каталога в промежуточных диапазонах;
- размеры межколоночных интервалов;
- масштаб заголовков и вертикальный ритм;
- допустимые различия компоновки между темами.

Breakpoint должен появляться там, где композиция перестаёт помещаться или читаться, а не только на традиционной ширине устройства. Текущие значения `640 px` и `1080 px` из прототипа считаются предварительными.

Предварительное решение для Каталога:

- 4 колонки на широком десктопе;
- 3 или 2 колонки на промежуточных экранах — уточняется на визуальном слое;
- 2 компактные колонки на смартфонах;
- акцентная карточка расчёта может занимать две мобильные колонки;
- длинные описания на смартфонах сокращаются или исключаются.

## 6. Визуальные направления

### A. «Завод-Гарант»

- монументальность;
- контрастные крупные плоскости;
- минимальные скругления;
- уверенная типографика;
- красный как редкий сигнал;
- акцент на статусе производителя.

### B. «Инженерная система»

- светлая модульная система;
- высокая читаемость;
- продуктовые изображения на чистом фоне;
- точная сетка;
- развитая работа с характеристиками и документами;
- базовый кандидат для расширения на продуктовые страницы.

### C. «Стальной контур»

- сочетание светлых и графитовых поверхностей;
- контурные паттерны;
- отсылки к сетке, периметру и инженерным чертежам;
- управляемая асимметрия;
- более выраженная бренд-идентичность.

Каждая концепция должна быть полноценной и адаптивной. Изменение только палитры не считается отдельной концепцией.

## 7. Политика изображений

- Для продукции используются реальные фотографии компании на светлом фоне.
- Для Hero и производственных секций допустимы стоки и AI-визуализации без ложного изображения конкретного завода.
- Старые реальные фотографии объектов по возможности улучшаются и масштабируются.
- Полностью сгенерированные кейсы не должны выдаваться за документальные фотографии выполненных объектов.
- Все изображения приводятся к согласованным пропорциям, цветовой обработке и уровню контраста внутри каждой концепции.

## 8. Дорожная карта

| ID | Этап | Результат | Статус |
| --- | --- | --- | --- |
| 0.1 | Изучение материалов компании и этапа прототипирования | Зафиксирован бизнес-, UX- и технический контекст | Завершено |
| 0.2 | Аудит HTML-прототипа | Найдены расхождения документации и реализации | Завершено |
| 0.3 | Фиксация формата результата | Код — основной презентационный макет, UI-kit позже кодом | Завершено |
| 0.4 | Фиксация компонентного подхода | Сквозные элементы из единого источника | Завершено |
| 0.5 | Сбор исходных визуальных материалов | Скриншоты прототипа, логотип, цвета, фотографии | Не начато |
| CP-1.1 | Создание чистого рабочего каркаса | Страницы, partial-компоненты, сборка, общие CSS/JS | Завершено |
| CP-1.1A | Базовый статический multi-page-каркас | Vite, Handlebars partials, страницы `index` и `catalog`, исходный прототип в `reference` | Завершено |
| CP-1.1B | Вынос сквозной структурной разметки | Общие header, мегаменю, мобильная навигация, footer и оболочка модального окна | Завершено |
| CP-1.1C | Перенос содержимого Главной и Каталога | Полный смысловой контент, порядок секций и карточек в page-specific partial-компонентах | Завершено |
| CP-1.1D | Заключительная техническая проверка и укрепление чистого каркаса | Воспроизводимое окружение, защищённые исходники и автоматическая проверка сборки | Завершено |
| CP-1.2 | Исправление демонстрационного интерактива | Модалка, состояния формы, меню, заглушки переходов | В работе |
| CP-1.2A | Доступное управление модальным окном расчёта | Общая модалка открывается всеми CTA, закрывается кнопкой, Escape и backdrop; фокус удерживается и возвращается | Завершено |
| CP-1.2B | Управление четырьмя десктопными мегаменю | Disclosure-навигация с единичным открытым состоянием и управлением мышью, touch и клавиатурой | Завершено |
| CP-1.2C | Следующий подэтап демонстрационного интерактива | Объём определяется отдельной постановкой | Не начато |
| CP-1.3 | Базовая responsive-система | Fluid-контейнер, сетка, токены, контрольные диапазоны | Не начато |
| 2.1 | Подбор типографических направлений | Веб-лицензируемые кириллические шрифтовые системы | Не начато |
| 2.2 | Подготовка визуалов | Реальные фото продукции, стоки, AI и восстановленные материалы | Не начато |
| 2.3 | Концепция A | Ключевые десктопные и мобильные экраны | Не начато |
| 2.4 | Концепция B | Ключевые десктопные и мобильные экраны | Не начато |
| 2.5 | Концепция C | Ключевые десктопные и мобильные экраны | Не начато |
| 2.6 | Проверка различимости концепций | Каждая система имеет собственную композицию и характер | Не начато |
| 3.1 | Главная во всех концепциях | Полная страница и интерактив | Не начато |
| 3.2 | Каталог во всех концепциях | Полная страница и интерактив | Не начато |
| 3.3 | Адаптив всех концепций | Проверенные ключевые ширины и состояния | Не начато |
| 4.1 | Визуальная полировка | Фото, типографика, отступы, переполнение, состояния | Не начато |
| 4.2 | Техническая проверка демо | Навигация, модалки, формы, темы, отсутствие явных ошибок | Не начато |
| 4.3 | Размещение стенда | Доступные ссылки на концепции, `noindex`, ограничение доступа при необходимости | Не начато |
| 5.1 | Выбор концепции заказчиком | Зафиксировано выбранное направление и замечания | Отложено |
| 5.2 | Кодовый UI-kit | Отдельная страница компонентов выбранной системы | Отложено |

## 9. Реестр решений

| ID | Дата | Решение | Основание | Статус |
| --- | --- | --- | --- | --- |
| D-001 | 2026-09-14 | Основной итоговый макет создаётся кодом | Интерактивная презентация лучше показывает адаптив и состояния; ответственность за трактовку договорного формата принимает исполнитель | Действует |
| D-002 | 2026-09-14 | UI-kit создаётся отдельной HTML-страницей после выбора концепции | Нет смысла полностью документировать три системы до выбора | Действует |
| D-003 | 2026-09-14 | Работа ведётся по структуре и текстам текущего прототипа | Другого утверждённого контента на текущем этапе нет | Действует |
| D-004 | 2026-09-14 | Не учитывать ограничения 1С-Битрикс при проектировании демонстрационного кода | Прототип не предполагается использовать как production-реализацию | Действует |
| D-005 | 2026-09-14 | Формы демонстрируют внешний вид и состояния без интеграции | Интеграция будет реализовываться отдельно на стороне production | Действует |
| D-006 | 2026-09-14 | Разработать минимум 2, целевым образом 3 полноценные концепции с адаптивом | Требование презентации заказчику | Действует |
| D-007 | 2026-09-14 | Сквозные компоненты хранятся в одном экземпляре и включаются при сборке | Необходимо единообразное тиражирование на страницы и темы | Действует |
| D-008 | 2026-09-14 | Сетка фиксируется двухэтапно: принципы сейчас, точные breakpoint во время визуальной работы | Типографика и медиа влияют на фактические точки перестроения | Действует |
| D-009 | 2026-09-14 | Для продукции использовать реальные фото; для производства допустимы стоки и AI | Качественные заводские и объектовые фотографии пока недоступны | Действует |
| D-010 | 2026-09-14 | Для каркаса выбран Vite 7 и `vite-plugin-handlebars`; HTML partials встраиваются только во время сборки | Нужны статические HTML-файлы и единый источник будущих сквозных компонентов без runtime `fetch()` | Действует |
| D-011 | 2026-09-14 | Мегаменю и мобильная навигация включаются внутрь `site-header` как вложенные build-time partials; футер и модальная оболочка подключаются страницами напрямую | Каждая страница должна получать полный сквозной хедер из одного источника и содержать каждый компонент ровно один раз | Действует |
| D-012 | 2026-09-14 | Модальное окно расчёта управляется отдельным vanilla JavaScript-модулем через нейтральные `data`-атрибуты | Несколько CTA должны безопасно использовать одну доступную модальную оболочку без inline-кода и сторонних библиотек | Действует |

### Артефакты CP-1.1A

- `package.json`, `package-lock.json` — npm-команды и зафиксированные версии зависимостей.
- `vite.config.js` — две HTML-точки входа и build-time подключение partial-шаблонов.
- `src/pages/index.html`, `src/pages/catalog.html` — нейтральные технические страницы.
- `src/partials/*.html` — пустые заготовки хедера, футера, четырёх desktop mega menus, мобильной навигации и модального окна расчёта.
- `src/styles/base.css`, `src/scripts/main.js` — точки входа будущих общих CSS и vanilla JavaScript.
- `src/assets/{fonts,icons,images}`, `src/styles/themes`, `docs` — подготовленные директории следующих подэтапов.
- `reference/prototype-original` — неизменённое содержимое `proto-stage-1.zip` для последующего сравнения.
- `.gitignore`, `.editorconfig`, `README.md` — базовые правила репозитория и команды работы.
- `dist/index.html`, `dist/catalog.html`, `dist/assets` — проверенный локально результат сборки; генерируется заново и не хранится в Git.

### Артефакты CP-1.1B

- `src/partials/site-header.html` — общий семантический хедер, который на этапе сборки включает четыре мегаменю и мобильную навигацию.
- `src/partials/mega-menu-*.html` — четыре самостоятельных дерева навигации по структуре и текстам исходного прототипа.
- `src/partials/mobile-navigation.html` — неинтерактивная оболочка корневого и четырёх вложенных уровней мобильного меню.
- `src/partials/site-footer.html` — единый футер с контактами, навигацией, реквизитами и правовой информацией.
- `src/partials/estimate-modal.html` — скрытая доступная оболочка диалога без формы и JavaScript-интерактива.
- `src/assets/images/logo-cascade.svg` — сохранённый без изменения актуальный SVG-исходник логотипа.
- `AGENTS.md` — устойчивые ограничения работы над проектом.
- Временные ссылки на ещё не созданные страницы ведут к именованным фрагментам; пробелы маршрутизации отмечены комментариями и не заполнялись новым коммерческим контентом.

### Артефакты CP-1.1C

- `src/partials/home` — семь секционных partial-компонентов Главной: первый экран, продукция, готовые решения, программы сотрудничества, выполненные объекты, производство с фактами и статическая форма расчёта.
- `src/partials/catalog/catalog-intro.html` — хлебные крошки, единственный заголовок первого уровня и вводное описание Каталога.
- `src/partials/catalog/category-grid.html` — полный исходный порядок из 11 товарных категорий и 12-й акцентной карточки расчёта.
- `src/pages/index.html` и `src/pages/catalog.html` сохранены компактными композициями общего хедера, одного `main`, page-specific partial-компонентов, общего футера и общей модальной оболочки.
- Для будущих фотографий оставлены семантические `figure` с нейтральными классами и доступными описаниями; изображения не создавались и не загружались.
- Неизвестные маршруты отмечены TODO-комментариями и используют единую техническую ссылку `#not-implemented`; новые продуктовые URL не выдумывались.
- Интерактив меню, модального окна и слайдера, обработка и валидация форм, состояния, визуальный слой, responsive-сетка, дизайн-токены и темы сознательно отложены. CP-1.1D, CP-1.2 и CP-1.3 не начинались.

### Финальная архитектура CP-1.1

- `src/pages/index.html` и `src/pages/catalog.html` — две композиционные входные страницы без дублирования сквозной разметки.
- `src/partials` — build-time partial-компоненты; общие header, навигация, footer и модальная оболочка находятся в корне, page-specific секции — в `home` и `catalog`.
- `src/styles/base.css` и `src/scripts/main.js` — единые CSS- и JavaScript-точки входа.
- Vite очищает `dist` и выпускает обе HTML-страницы в его корень, а локальные ресурсы — в `dist/assets`; относительная база сохраняет работоспособность во вложенной директории.
- Поддерживается Node.js 22 (не ниже 22.12); установка выполняется через `npm ci`, сборка — `npm run build`, комплексная проверка — `npm run verify`, локальный просмотр — `npm run preview`.
- Исходные тексты размещены в `docs/source`, архив — в `reference/source-archives`, фирменный SVG — в `reference/brand`, распакованный оригинал — в `reference/prototype-original`; контрольные суммы записаны в `reference/SHA256SUMS`.
- CP-1.2, CP-1.3 и визуальный слой не начаты.

### Артефакты CP-1.2A

- `src/scripts/components/modal.js` — открытие общей модалки несколькими триггерами, синхронизация `hidden` и `aria-hidden`, блокировка прокрутки, закрытие кнопкой, Escape и кликом по backdrop, focus trap и возврат фокуса.
- `src/scripts/main.js` — компактная точка инициализации модального модуля.
- `src/partials/estimate-modal.html` и CTA хедера, мобильной навигации, Hero и Каталога связаны нейтральными `data`-атрибутами, `aria-controls` и `aria-haspopup="dialog"`.
- `src/styles/base.css` содержит только технические правила скрытия, блокировки прокрутки, геометрии backdrop и различимого keyboard focus; визуальный дизайн не создавался.
- `scripts/verify-build.mjs` проверяет единственность и структуру модалки, цели триггеров, тип кнопки закрытия и локальное подключение собранного JavaScript.
- CP-1.2B и последующие подэтапы не начинались; форма, меню, слайдер и обработка `#not-implemented` не изменялись.

### Артефакты CP-1.2B

- `src/scripts/components/mega-menu.js` — независимый и идемпотентный disclosure-контроллер четырёх десктопных мегаменю; одновременно открывается только связанная с активной кнопкой панель.
- Mouse-сценарий для точного указателя: отложенное открытие по `pointerenter`, сохранение панели при переходе между кнопкой и панелью, отложенное закрытие при уходе и немедленное закрытие внешним `pointerdown`.
- Touch-сценарий использует нативный клик по кнопке: первый клик открывает панель, повторный закрывает, выбор другой кнопки переключает единственную открытую панель.
- Keyboard-сценарий сохраняет обычный Tab-порядок без focus trap: Enter и Space работают нативно, ArrowDown открывает панель и фокусирует первую ссылку, ArrowLeft/ArrowRight циклически перемещают фокус по кнопкам, Escape закрывает панель и возвращает фокус.
- Кнопки и панели связаны через уникальные `id`, `aria-controls` и `aria-labelledby`; состояния `hidden`, `aria-hidden`, `aria-expanded` и технические `data-mega-menu-state`/`data-mega-menu-open` синхронизированы.
- `scripts/verify-build.mjs` проверяет количество, связи и исходные состояния мегаменю, отсутствие ARIA menu-ролей и локальное подключение JavaScript. Визуальный слой, мобильное drill-down-меню и CP-1.2C не затрагивались.

## 10. Известные проблемы исходного прототипа

- Разметка модального окна отсутствует, хотя CSS и JavaScript для него существуют.
- Мобильный Каталог реализован одной колонкой вместо описанных в документации двух.
- Общий JavaScript полностью продублирован в двух HTML-файлах.
- Большое количество ссылок-заглушек не даёт обратной связи при клике.
- Демонстрационная отправка формы реализована системным `alert`.
- Глобально отключён стандартный `outline`, но не создана полноценная замена.
- Размеры контейнера, хедера и некоторые токены расходятся между документами и кодом.
- Фирменный красный указан разными HEX-значениями.
- В прототипе отсутствуют реальные изображения.

## 11. Ожидаемые материалы

- полный скриншот Главной при ширине 1440 px;
- полный скриншот Главной при ширине около 390 px;
- полный скриншот Каталога при ширине 1440 px;
- полный скриншот Каталога при ширине около 390 px;
- открытое мегаменю «Продукция» на десктопе;
- корневое и вложенное мобильное меню;
- исходник актуального логотипа;
- точные HEX-коды фирменных цветов;
- фотографии продукции;
- старые фотографии производства и объектов, если доступны.

## 12. Открытые вопросы

| ID | Вопрос | Влияние | Статус |
| --- | --- | --- | --- |
| Q-001 | Какой HEX-код красного является актуальным? | Все три визуальные системы | Открыт |
| Q-002 | Какой исходник логотипа считать финальным? | Хедер, футер, мобильное меню | Открыт |
| Q-003 | Какие реальные фотографии продукции доступны для макета? | Карточки Главной и Каталога | Открыт |
| Q-004 | Достаточно ли отличаются три выбранных направления после первого дизайн-спринта? | Решение о продолжении концепции C | Будет проверено |

## 13. Критерии готовности текущего этапа

- две обязательные концепции полностью готовы;
- третья концепция готова, если она обеспечивает самостоятельное направление;
- Главная и Каталог существуют во всех представляемых концепциях;
- сквозные компоненты подключаются из единого источника;
- страницы проверены на 360, 390, 768, 1024, 1440 и 1920 px;
- меню, модалка, форма и слайдер демонстрируют предусмотренные состояния;
- визуальные концепции отличаются не только палитрой;
- реальные фото продукции используются последовательно;
- отсутствуют очевидно сломанные интерактивные элементы;
- для каждой концепции существует отдельная демонстрационная ссылка;
- стенд защищён от индексации;
- выбранное направление и замечания заказчика можно зафиксировать в документе.

## 14. Следующий шаг

1. Получить скриншоты и визуальные исходники.
2. Создать чистый компонентный каркас проекта.
3. Исправить интерактивные дефекты прототипа.
4. Подготовить базовую responsive-систему и механизм тем.
5. Перейти к первому визуальному спринту трёх концепций.

## 15. Журнал изменений

### 2026-09-14 — CP-1.2B

- CP-1.2 оставлен в статусе «В работе», CP-1.2B завершён, CP-1.2C зафиксирован как «Не начато».
- Четыре десктопных мегаменю реализованы как disclosure navigation без `menu`, `menuitem` и `menubar`: их обычные ссылки, списки, тексты и порядок сохранены.
- Добавлены взаимоисключающее открытие, закрытие по Escape, внешнему указателю и уходу фокуса, циклическое перемещение по кнопкам стрелками и переход к первой ссылке по ArrowDown.
- Для fine pointer добавлены короткие задержки открытия и закрытия; touch продолжает использовать click, а Tab и Shift+Tab — естественный порядок документа.
- Расширена автоматическая проверка сборки; мобильное меню, формы, слайдер, `#not-implemented`, responsive- и визуальный слои не изменялись.

### 2026-09-14 — CP-1.2A

- CP-1.2 переведён в статус «В работе», внутренний подэтап CP-1.2A завершён; CP-1.2B оставлен в статусе «Не начато».
- Все CTA расчёта в общем хедере и мобильной навигации, Hero Главной и акцентной карточке Каталога открывают единственную общую оболочку `estimate-modal`.
- Реализованы закрытие кнопкой, Escape и кликом по backdrop без реакции на клик внутри диалога, блокировка прокрутки, начальная установка, циклическое удержание и возврат фокуса.
- Добавлены структурные проверки модалки и локального JavaScript; полноценная форма, остальные интерактивы и визуальный слой не затрагивались.

### 2026-09-14 — CP-1.1D

- CP-1.1 и CP-1.1D переведены в статус «Завершено»; CP-1.2 оставлен в статусе «Не начато».
- Проверена архитектура результатов CP-1.1A–C, устранены корневые URL переходов и включена относительная база Vite для публикации во вложенной директории.
- Исходные материалы организованы в `docs/source`, `reference/source-archives` и `reference/brand` через `git mv` без изменения контрольных сумм.
- Добавлены `.gitattributes`, `reference/SHA256SUMS`, Node.js 22 в `.nvmrc` и `engines`, а также независимая от сторонних библиотек команда `npm run verify`.
- Подтверждены очистка `dist`, раскрытие build-time partials, структура обеих страниц, обязательные секции Главной, состав и порядок 12 карточек Каталога и существование локальных ресурсов.
- Интерактив, `#not-implemented`, responsive-сетка, дизайн-токены, темы и визуальная стилизация не изменялись.

### 2026-09-14 — CP-1.1C

- CP-1.1 оставлен в статусе «В работе», подэтап CP-1.1C завершён; CP-1.1D не начат.
- На Главную в исходном порядке перенесены первый экран, основная продукция, готовые решения, программы сотрудничества, выполненные объекты, производственный блок с преимуществами и фактами, секция запроса расчёта.
- В Каталог перенесены хлебные крошки, вводный блок, 11 товарных категорий в исходном порядке и акцентная 12-я карточка запроса расчёта.
- Крупные секции вынесены в `src/partials/home` и `src/partials/catalog`; входные страницы остались файлами-композициями и используют сквозные partial-компоненты CP-1.1B.
- Форма расчёта добавлена только как статическая семантическая разметка. Интерактив, валидация, отправка, состояния, визуальный и адаптивный слои отложены до соответствующих этапов.

### 2026-09-14 — CP-1.1B

- CP-1.1 оставлен в статусе «В работе», подэтап CP-1.1B завершён; CP-1.1C не начат.
- Структурная разметка хедера, четырёх мегаменю, мобильной навигации, футера и модальной оболочки перенесена в build-time partial-компоненты.
- Обе входные страницы подключают общий хедер, футер и модальную оболочку без дублирования; вложенные навигационные partials подключаются только из хедера.
- Логотип вынесен в `src/assets/images` и используется хедером и футером.
- Интерактив, формы, адаптив и визуальная стилизация сознательно не реализовывались.

### 2026-09-14 — CP-1.1A

- Этап CP-1.1 переведён в статус «В работе»; внутренний подэтап CP-1.1A завершён.
- Исходный HTML-прототип извлечён без изменений в `reference/prototype-original`.
- Создан чистый multi-page-каркас на Vite, HTML, CSS и vanilla JavaScript.
- Подключён `vite-plugin-handlebars` для build-time partial-шаблонов и созданы пустые заготовки будущих сквозных компонентов.
- Добавлены входные страницы Главной и Каталога, конфигурация, служебные директории и документация команд.
- Установка зависимостей и production-сборка выполнены успешно.
- Визуальный слой, адаптив, токены, интерактив, формы и механизм тем отложены до соответствующих подэтапов; CP-1.1B не начат.

### 2026-09-14 — версия 1

- Создан живой документ проекта.
- Зафиксирован кодовый формат результата.
- Зафиксировано создание UI-kit после выбора концепции.
- Зафиксирована обязательная компонентность сквозных элементов.
- Сформулирована двухэтапная политика сетки и breakpoint.
- Добавлена дорожная карта, реестр решений, проблемы прототипа и критерии готовности.
